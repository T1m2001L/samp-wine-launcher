# Normalize line endings:
#  - shell scripts and config MUST stay LF, otherwise they break on Linux
#  - everything else: let git handle it
* text=auto
*.sh    text eol=lf
*.conf  text eol=lf
LICENSE text eol=lf
